/**
 * Configuration Module
 * 
 * Centralized configuration for the Family Money Tracker app.
 * Import from '@/lib/config' in your code.
 */

export {
  env,
  getAppEnvironment,
  getEnvironmentLabel,
  getEnvironmentColor,
  validateEnv,
  type AppEnvironment,
} from './env';
